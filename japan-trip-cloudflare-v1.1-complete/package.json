{
  "name": "japan-trip-2026",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute japan-trip-db --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute japan-trip-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
